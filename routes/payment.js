// const { Router } = require("express");
// const router = Router();

// const payment = require("../controllers/payment.controller");

// router.post("/", payment.createPaymentIntent);

// router.post("/stripe", payment.stripeWebHook);

// module.exports = router;
